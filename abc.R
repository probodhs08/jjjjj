find out the loc of current working directory define any 
three variables save those variables in the file with 
name abc into some other directory
list down all the variables and remove the first three variable
names
open a csv file and store one data frame consisting the records 
of three students
display the file


getwd()
x<-1
y<-2
z<-3
save(x,y,z,file=)